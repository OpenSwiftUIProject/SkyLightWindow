# TODO

After [tuist/tuist#11799](https://github.com/tuist/tuist/pull/11799) is merged and included in a stable Tuist release:

- [ ] Add a root `mise.toml` that pins the first Tuist release containing native package trait support.
- [ ] Update `README.md` to replace the pull-request build requirement with the minimum released Tuist version and its installation instructions.
- [ ] Regenerate and build both `MoveToSky` examples with the pinned Tuist release.
